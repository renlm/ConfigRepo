seata:
  enabled: true
  applicationId: ${spring.application.name}
  txServiceGroup: myTxGroup
  service:
    vgroupMapping:
      myTxGroup: SEATA-SERVER
    grouplist:
      SEATA-SERVER: http://localhost:8099